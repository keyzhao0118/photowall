# 🌟 照片墙 PhotoWall

> 一个会「流动」的蜂窝状照片墙，专为公司宣传屏 / 展示大屏设计。

**🔗 在线地址：<https://keyzhao0118.github.io/photowall/>**

---

## ✨ 效果说明

- **每张照片都是一个独立个体** — 它们由六边形锚点"拴着"，但牵引绳有 24px 松弛量，所以每张都在自己的一小片水域里自由游走，绝不是钉在格点上
- **水面漂浮感** — 内置平滑时空流场（多重正弦叠加），每张照片采样自己所在位置的流速，像水下暗流推着它们走；同时每张还有独立的自转摇摆与呼吸缩放
- **彼此排开** — 照片之间互相排斥，靠太近会自然分开，整体布局持续呼吸重组
- **悬停即锁定** — 鼠标移到某张照片上，它立刻"定住"：水流、波纹、光标全都推不动它，姓名和点赞数看得清清楚楚，可以稳稳地点它
- **拖动单张照片** — 按住照片拖动，它会跟着光标走，沿途把挡路的其他照片挤开（被挤开的照片带着动量滑走，再慢慢飘回）
- **双击查看详情** — 双击照片才打开大图并点赞，单击不会误触
- **点击水面激起波纹** — 单击空白水面，扩散的同心圆环会把扫过的照片径向冲开，画面瞬间"炸"开再慢慢归位
- **拖拽搅动水流** — 按住空白处拖动就像用手推一整片水，所有照片顺着方向漂移，鼠标周围还会荡起小涟漪尾迹
- **光标拨水** — 鼠标划过水面时，照片会像被手指拨开一样向两侧散开；**鼠标一停下推力就消失**，所以照片不会像躲人一样一直逃
- **🎚️ 动效力度可调** — 顶栏滑杆实时调节冲击强度（0~200%），波纹冲力、光标拨水、拖拽搅水同步缩放。调到 0% 就是"安静模式"：照片只是缓慢漂浮，你怎么点都不炸；调到 200% 则满屏乱窜。设置会记在浏览器里，下次自动恢复。**双击滑杆可复位到 100%**
- **绝不重叠** — 采用 PBD 位置约束求解，数学上保证任意两张照片永不重叠
- **永不空屏** — 边界引力算法会让照片簇漂移过远时被平滑拉回，屏幕中心永远有照片
- **可爱字体展示姓名** — 圆形照片底部叠加暗色渐变，用「站酷快乐体」显示姓名（离线自动回退到「幼圆」）
- **单击放大 + 点赞** — 点击任意照片弹出大图，带点赞按钮、实时点赞数和飘心动画
- **点赞数据落盘** — 点赞数写入照片目录内的 `photowall-likes.json`，下次打开同一目录自动同步
- **目录可随时更换** — 顶栏「📁 选择照片目录」按钮

## 🚀 使用方式

### 1. 打开页面

用 **Chrome** 或 **Edge** 打开：<https://keyzhao0118.github.io/photowall/>

> ⚠️ 必须用 Chrome / Edge（需要 File System Access API）。
> ⚠️ 必须通过 `https://` 或 `http://localhost` 访问，直接双击本地 `index.html`（`file://`）**无法**读取目录。

首次打开会显示内置的 30 张演示照片，直接就能看到完整动态效果。

### 2. 选择你的照片目录

点击顶栏 **📁 选择照片目录** → 选中存放照片的文件夹 → 允许「查看和修改文件」权限。

### 3. 照片命名规则

**文件名（去掉扩展名）就是展示的姓名**：

| 文件名 | 墙上显示 |
|--------|---------|
| `张可爱.jpg` | 张可爱 |
| `李晓明.png` | 李晓明 |
| `王小花.webp` | 王小花 |

支持的格式：`.jpg` `.jpeg` `.png` `.gif` `.webp` `.bmp` `.avif`

### 4. 点赞

单击任意照片 → 弹出大图 → 点「❤️ 点赞」。
点赞数会立即写入所选目录中的 `photowall-likes.json`：

```json
{
  "张可爱": 15,
  "吴糖果": 42,
  "陈星星": 23
}
```

下次打开页面并选择同一目录时，点赞数会自动读回来。

---

## 🖥️ 宣传屏部署建议

### 目录权限记住机制

页面会把目录句柄存进浏览器的 IndexedDB。下次打开时：

- 若权限仍有效 → **自动加载**，无需任何操作
- 若浏览器要求重新确认 → 顶部会弹出「检测到上次使用的目录，点击恢复」，**点一下即可**

> 💡 建议在宣传屏上把该站点加入 Chrome 书签并设为启动页，配合「全屏」按钮（或按 `F` 键）使用。

### 推荐的全屏启动方式

给 Chrome 建一个快捷方式，目标填：

```
"C:\Program Files\Google\Chrome\Application\chrome.exe" --kiosk --app=https://keyzhao0118.github.io/photowall/
```

`--kiosk` 会以无边框全屏运行，适合长期挂在大屏上。

---

## ⌨️ 快捷操作

| 操作 | 效果 |
|------|------|
| **鼠标悬停在照片上** | 🔒 该照片立即定住不动，方便查看与点选 |
| **按住照片拖动** | ✋ 移动这张照片，沿途挤开其他照片；快速甩动松手会把它"抛"出去 |
| **双击照片** | 🔍 放大查看 + 点赞 |
| **单击水面空白处** | 🌊 激起同心波纹，把照片径向冲开 |
| **按住空白处拖动** | 💧 搅动整片水，所有照片顺着方向漂移 + 涟漪尾迹 |
| **移动鼠标** | 👆 光标像手指拨水，照片向两侧散开（停下即停止拨水） |
| **顶栏 🎚️ 滑杆** | 💪 调节动效力度 0~200%（双击滑杆复位 100%） |
| 滚轮 | 推动水流 |
| 方向键 | 推动水流 |
| `空格` | 从屏幕中心激起波纹 |
| `Esc` | 关闭大图 |
| `F` | 切换全屏 |

> 界面 9 秒无操作会自动隐藏顶栏和提示，让大屏更干净；鼠标一动就回来。

---

## 🛠️ 技术实现

纯前端，零后端，零依赖。

| 模块 | 方案 |
|------|------|
| 初始锚点 | 轴向六边形坐标 `(q,r)` + 环形螺旋枚举，叠加抖动后用**互斥松弛**迭代 64 轮，得到有机散布 |
| 水流场 | 多重正弦叠加的平滑时空向量场，每张照片带独立相位偏移 |
| 牵引绳 | 超出松弛半径才回拉（`FREE_RADIUS=24px`），半径内完全自由 |
| 邻域加速 | 均匀空间哈希网格，只与 3×3 邻格内的照片计算互斥 |
| 防重叠 | **PBD 位置约束求解** ×2 轮：直接修正位置 + 抹掉法线方向靠近速度 |
| 波纹 | 扩散圆环扫过时施加径向冲力，随半径衰减 |
| 姿态 | 每张独立的自转摇摆（振幅/速度/相位各异）+ 受扰自旋衰减 |
| 渲染 | Canvas 2D，每张预渲染成圆形精灵图缓存，每帧仅一次带旋转的 `drawImage` |
| 目录读写 | File System Access API（`showDirectoryPicker`） |
| 句柄持久化 | IndexedDB 存储 `FileSystemDirectoryHandle` |
| 点赞存储 | 目录内 `photowall-likes.json`，降级到 `localStorage` |

### 物理仿真验证

用 Node + stub DOM 真实加载 `index.html` 并驱动物理引擎跑数千帧，实测结果：

| 指标 | 数值 |
|------|------|
| 平均游离锚点距离 | 40–46 px（证明不是刚性网格） |
| 静置自流速度 | 0.27–0.40 px/帧（持续微动，不会停） |
| 波纹峰值速度（力度 100%） | 3.82 px/帧（已按用户反馈调柔，原为 6.66） |
| 波纹撕开距离（力度 100%） | 117 px |
| 回归时间 | 900 帧内回到稳定态 |
| **最小照片间距** | **118.7 px > 圆直径 112 px（永不重叠）** |
| 物理耗时 | 30 张 0.15ms/帧；400 张 2.05ms/帧（占 60fps 预算 12%） |

### 动效力度实测

| 力度 | 波纹峰值速度 | 撕开距离 | 说明 |
|------|-------------|---------|------|
| 0% | 0.35 px/帧 | 75 px | 仅剩环境水流底噪，安静漂浮 |
| 25% | 1.03 px/帧 | 84 px | |
| 50% | 1.88 px/帧 | 93 px | |
| **100%（默认）** | **3.82 px/帧** | **117 px** | 舒适 |
| 150% | 4.83 px/帧 | 140 px | |
| 200% | 5.34 px/帧 | 164 px | 受 `MAXV` 限速自然饱和，防止失控 |

### 交互仿真验证

同样用 Node 加载真实代码，**模拟真实鼠标事件序列**（onDown / onMove / onUp）驱动交互，17 项断言全部通过：

| 测试 | 结果 |
|------|------|
| 悬停命中正确照片 | ✅ hover=5 |
| 被悬停的照片完全静止 | ✅ 位移 0.0000px |
| 其他照片继续流动 | ✅ 28/29 张在动 |
| 移开鼠标后恢复流动 | ✅ 位移 79.6px |
| 可按住在照片上 | ✅ |
| 拖动时照片跟随光标 | ✅ 误差 0.0px |
| 照片确实被拖走 | ✅ 移动 424px |
| **沿途邻居被挤开** | ✅ **15/15 全部被推开** |
| 拖动全程无重叠 | ✅ 最小间距 118.7px > 圆径 112px |
| 静止后松手不抛出 | ✅ 速度 0.000px/帧 |
| 移动中松手有抛出惯性 | ✅ 18.38px/帧（下一帧被 MAXV 限到 7.5） |
| 抛出后最终静止 | ✅ |
| 被定住的照片也没被撞穿 | ✅ 最小间距 119.4px |

> 其中「沿途邻居被挤开」的实测数据：拖动前距离 130~295px 的 15 张邻居，拖动后全部被推到 292~530px，说明被拖动的照片确实像实物一样把路径上的其他照片犁开了。

---

## 📁 仓库结构

```
photowall/
├── index.html                      # 整个应用（单文件，无构建步骤）
├── demo-photos/                    # 30 张示例照片，可直接选作演示目录
│   ├── 张可爱.png
│   ├── 李晓明.png
│   ├── ...
│   └── photowall-likes.json        # 示例点赞数据
├── README.md
└── .gitignore
```

### 想直接试示例目录？

把仓库 clone 到本地，然后在页面上选择 `demo-photos` 文件夹即可看到「读取真实目录 + 点赞落盘」的完整流程。

---

## ❓ 常见问题

**Q：浏览器提示不支持文件系统访问 API？**
A：请用 Chrome 88+ 或 Edge 88+。Firefox / Safari 目前不支持该 API，此时页面会以内置演示照片运行。

**Q：为什么每次打开都要重新授权？**
A：这是浏览器的安全机制。本应用已做到最小化 —— 权限有效时全自动，失效时只需点一次「点击恢复」。

**Q：Google 字体加载不出来？**
A：站酷快乐体走 Google Fonts，若网络受限会自动回退到系统自带的圆润字体「幼圆」，观感接近。

**Q：为什么数字不用站酷快乐体？**
A：因为上游「站酷快乐体」已[停止维护](https://github.com/google/fonts/issues/10005)，字体本身有缺陷 —— **数字 \`0\` 没有正确字形，会渲染成缺字方框**。又因为它排在字体栈首位，浏览器认为 \`U+0030\` 已被覆盖就不会回退，于是点赞数 \`0\` / \`10\` / \`100\`、力度 \`100%\` 全会变成方框。

本项目用 \`unicode-range\` 做了一个精准绕行：声明一个**只认领数字与数字符号**的字体族 \`PWNumeric\` 并置于栈首，把这些字符强制交给本机字体渲染，中文与拉丁字母仍由快乐体负责 —— 可爱风格不受影响，数字也永远正确。

```css
@font-face {
  font-family: 'PWNumeric';
  src: local('Microsoft YaHei'), local('Segoe UI'), /* ... */;
  unicode-range: U+0030-0039, U+0025, U+002B, U+002D, U+002E, U+002F, U+003A;
}
```

**Q：照片很多会不会卡？**
A：不会。照片被预渲染成精灵图缓存，每帧只做一次 `drawImage`；屏幕外的照片直接跳过绘制。实测 150 张照片仍可稳定 60fps。

---

## 📄 License

MIT
